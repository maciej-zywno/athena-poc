= simple_form_for [@game] do |f|
  = f.input :name, required: true, label: 'Invocation Name'
  = f.input :patient_id, required: true, collection: current_user.kids.pluck(:id), label: 'Kid Id'
  -#= f.input :patient_id, required: true, collection: current_user.kids.pluck(:id)
  = f.submit class: 'btn btn-primary'